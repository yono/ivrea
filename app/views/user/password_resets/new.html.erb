<div class="jumbotoron">
  <h1>パスワードリセット</h1>
  <%= form_with(model: User.new, url: {action: 'create'}, method: :post, data: {remote: false}) do |form| %>

    <div class="form-group">
      <label>メールアドレス</label>
      <%= form.text_field :email, clsss: 'form-control' %>
    </div>
    <%= form.submit "送信する" %>
  <% end %>
</div>
